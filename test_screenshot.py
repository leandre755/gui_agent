import asyncio
import os
from gui_agent.server import gui_web_action

async def main():
    print("Testing screenshot fix")
    # playwright test requires the browser binaries installed
    # We will just verify imports and syntax

asyncio.run(main())
